<?xml version="1.0" encoding="UTF-8"?>
<manifest>

  <remote  name="aosp"
           fetch="https://android.googlesource.com/" />
  <remote  name="github"
           fetch=".." />
  <default revision="master"
           remote="github"
           sync-c="true"
           sync-j="4" />

  <!-- from github port repositories -->
  <!-- EPGQ -->
  <project path="epgq/build" name="build" groups="armemu" />
  <project path="epgq/bo" name="bo" groups="armemu" />
  <project path="epgq/armemu" name="armemu" groups="armemu" />
  <project path="epgq/cm_armemu" name="cm_armemu" groups="armemu" />

  <!-- ASP -->
  <project path="asp/x86emu" name="x86emu" groups="x86emu" />

  <!-- from original Android repositories -->

</manifest>
